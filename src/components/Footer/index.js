import styles from './Footer.module.css'

const Footer = () => {
    return(
        <footer className={styles.footer}>
            <h2> Desenvolvido por Gabriel Ramalho</h2>
        </footer>
    )
}

export default Footer;